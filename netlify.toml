[build]
  command = "npm i --no-save @tailwindcss/oxide-linux-x64-gnu lightningcss-linux-x64-gnu && npx prisma generate && npx prisma db push --accept-data-loss && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
